@model IEnumerable<Cinema.Areas.Admin.Models.RapModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IDRap)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TenRap)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrangThai)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IDRap)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TenRap)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrangThai)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDRap">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDRap">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDRap">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
