@model IEnumerable<Cinema.Areas.Admin.Models.KhungGioModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IDPhim)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThoiGian)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrangThai)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IDPhim)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ThoiGian)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrangThai)
            </td>

            <td>
                <a asp-action="Edit" asp-route-id="@item.IDKG">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDKG">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDKG">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
