@model IEnumerable<Cinema.Areas.Admin.Models.GheModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IDGhe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TenGhe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SoGhe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrangThai)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IDGhe)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TenGhe)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SoGhe)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrangThai)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDGhe">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDGhe">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDGhe">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
